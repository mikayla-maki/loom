# AGENTS.md

## Cursor Cloud specific instructions

This repository currently contains only a CC0 1.0 Universal `LICENSE` file. There is no application code, no dependencies, and no build/test/lint configuration.

- **No services to run.** The repo has no `package.json`, `requirements.txt`, `Cargo.toml`, `go.mod`, `docker-compose.yml`, or any other project manifest.
- **No update script actions needed.** Future agents should re-evaluate once application code is added.
- When code is added to this repository, update this file with setup, build, lint, test, and run instructions.
